/**
 * API del juego (Spring Boot + MySQL + JWT). Expone recursos bajo {@code /api/v1/…} y {@code /auth};
 * el cliente Phaser/React consume JSON y envía el token en cada petición protegida.
 */
package com.proyecto.pokemon_backend;
