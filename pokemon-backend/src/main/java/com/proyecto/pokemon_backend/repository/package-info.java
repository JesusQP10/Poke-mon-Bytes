/**
 * Acceso a datos vía Spring Data JPA. Las consultas JPQL o nativas van declaradas en la interfaz
 * cuando un {@code findBy…} no basta (p. ej. borrados masivos por usuario).
 */
package com.proyecto.pokemon_backend.repository;
