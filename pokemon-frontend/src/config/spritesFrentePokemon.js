/**
 * Sprites frontales locales por pokedexId.
 * 
 * Si el id no aparece en este mapa, se carga automáticamente desde PokéAPI Crystal (gen-II).
 *
 *
 * NPCs de la sala debug:
 *   10  → Caterpie    (npc_captura)
 *   163 → Hoothoot    (npc_battle_normal)
 *   79  → Slowpoke    (npc_battle_confuso)
 *   39  → Jigglypuff  (npc_battle_dormido)
 *   58  → Growlithe   (npc_battle_quemado)
 *   23  → Ekans       (npc_battle_veneno)
 *   179 → Mareep      (npc_battle_paralisis)
 *   86  → Seel        (npc_battle_congelado)
 *   137 → Porygon     (captura)
 *
 * @type {Record<number, string>}
 */
export const SPRITES_FRENTE_LOCAL = {
  
};
